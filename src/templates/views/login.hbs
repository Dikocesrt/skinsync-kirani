<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SkinSync Login</title>

    {{> font}}

    {{> css}}
</head>
<body class="bg-secondary-pink min-h-screen flex justify-center items-center">

    <form class="flex flex-row bg-white w-fit rounded-[12px]" action="/login" method="POST">
        <img src="/img/login.png" alt="" class="hidden lg:block w-[370px] rounded-l-[12px]">

        <div class="flex flex-col gap-y-[10px] lg:justify-center p-[24px] lg:py-0 lg:px-[60px]">
            <p class="font-poppins text-[16px] text-primary-pink font-semibold mb-1">SELAMAT DATANG KEMBALI BESTIE</p>

            {{#if error}}
                <div class="flex w-full justify-center items-center py-2 border rounded-[4px] bg-red-300 border-red-500">
                    <p class="font-poppins text-[12px] text-red-500 font-semibold">{{error}}</p>
                </div>
            {{/if}}
            
            <div class="flex flex-col gap-y-[4px]">
                <label for="email" class="font-poppins text-[12px] text-primary-pink font-semibold">Email</label>
                <input required type="email" name="email" id="email" class="w-full px-3 border-2 border-[#C4C4C4] rounded-[8px] shadow-lg py-[6px] focus:outline-primary-pink text-[12px] font-poppins">
            </div>

            <div class="flex flex-col gap-y-[4px] mb-1">
                <label for="password" class="font-poppins text-[12px] text-primary-pink font-semibold">Password</label>
                <input required type="password" name="password" id="password" class="w-full px-3 border-2 border-[#C4C4C4] rounded-[8px] shadow-lg py-[6px] text-[12px] focus:outline-primary-pink font-poppins">
            </div>

            <button type="submit" class="bg-primary-pink py-[7px]  px-4 rounded-[8px]">
                <p class="font-poppins text-[12px] text-white font-semibold">Login</p>
            </button>

            <p class="font-poppins text-[12px] text-primary-gray text-center">Belum punya akun? <a href="/register" class="font-poppins text-[12px] text-primary-pink font-semibold underline">Daftar</a></p>

        </div>

    </form>
    
</body>
</html>