<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar Riwayat</title>

    {{> font}}
    {{> css}}
</head>
<body class ="bg-white">

    <!-- NAVBAR -->
    <div class="w-full bg-secondary-pink">
        <div class="w-full max-w-[350px] lg:max-w-[1050px] 2xl:max-w-[1500px] mx-auto">
            {{> navbar}}
       </div>
    </div>

    <!-- PWMBUNGKUS ISI -->
    <div class="w-full max-w-[350px] lg:max-w-[1050px] 2xl:max-w-[1500px] mx-auto py-[20px]">
        <!-- HEADER -->
        <div class="flex flex-col gap-y-[12px]">

            <p class="font-poppins text-[20px] text-primary-pink font-bold lg:text-[20px]">RIWAYAT CERITA & REKOMENDASI</p>

            {{#if histories.length}}
            <div class="grid grid-cols-1 lg:grid-cols-3 2xl:grid-cols-4 gap-x-[12px] gap-y-[12px]">
                {{#each histories}}
                <div class="flex flex-row gap-x-[16px] bg-white rounded-[12px] border-2 border-primary-pink px-[12px] py-[8px]">
                    <img src="{{this.skin_type.image}}" alt="" class="w-[150px] rounded-[12px]">
                    
                    <div class="flex flex-col gap-y-[4px]">
                        <p class="font-poppins text-[16px] text-primary-black font-bold uppercase">{{this.skin_type.name}}</p>
                        <p class="font-poppins text-[11px] text-primary-gray uppercase">{{this.createdAt}}</p>
                        <p class="font-poppins text-[11px] text-primary-gray">{{truncate this.result 70}}</p>
                        <a href="/histories/{{this.id}}" class="border-2 border-primary-pink bg-primary-pink rounded-[6px] w-full py-[4px] flex items-center justify-center">
                            <p class="font-poppins text-[12px] text-white font-semibold">LIHAT DETAIL</p>
                        </a>
                    </div>
                </div>
                {{/each}}
            </div>
            {{else}}
            <p class="font-poppins text-[14px] text-primary-gray font-semibold">Belum ada riwayat</p>
            {{/if}}
        </div>
    </div>
    

    {{> navbarScript}}
</body>
</html>